The assignment is done using Polymer framework.

Prequisites to run the project:
Install Polymer-CLI : npm install -g polymer-cli

Start the development server : polymer serve
The above command serves the app at `http://127.0.0.1:8081`